{
  "name": "@magistra/index",
  "version": "0.0.1",
  "private": true,
  "description": "Interfaccia tipizzata dell'indice del corpus normativo e implementazione LanceDB read-only (apertura, manifest, query ANN con prefiltro per metadato).",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.31.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "apache-arrow": "^21.1.0"
  }
}
